# Netlify config for CRA SPA
[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  REACT_APP_API_URL = "https://gym-checkin-backend.fly.dev/api"

# SPA fallback routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
